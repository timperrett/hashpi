#!/usr/bin/env ansible-playbook -i inventory.ini --ask-pass
---
- name: bootstrap the pi clusters
  gather_facts: true
  remote_user: pi
  become: yes
  hosts:
    - leaders
    - followers
  roles:
    - users
    - shell
